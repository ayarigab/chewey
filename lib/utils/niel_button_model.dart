// ignore_for_file: constant_identifier_names

enum NielButtonSize { XS, S, M, L, XL }

enum NielButtonState { DEFAULT, DISABLED }

enum NielButtonType { PRIMARY, SECONDARY }

enum GradientDirection { horizontal, vertical, diagonal }
